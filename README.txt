Discord bot that can join any voice channel. 

* To listen to user commands, user needs to have role "adminid".

Commands:
    - When user is in voice channel, using "/join" will join bot into that channel
    - When user is in voice channel, using "/leave" will disconnect bot


Testing bot:
    - run "node main.js" in root folder